import {Route} from "../model/Route.ts";
import {Vehicle} from "../model/Vehicle.ts";

export function calculateRoutePriceWithCar(route: Route, vehicle: Vehicle): number {
    throw new Error("Not Implemented")
}